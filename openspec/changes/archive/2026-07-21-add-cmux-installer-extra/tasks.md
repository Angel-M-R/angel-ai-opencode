## 1. Extra Selection

- [x] 1.1 Add a cmux extra descriptor with an explicit false interactive default while preserving the current true defaults of every existing extra.
- [x] 1.2 Initialize TUI extra selections from descriptor defaults and preserve explicit select-all and `--all` behavior.

## 2. Vendored Integration Assets

- [x] 2.1 Add exact `cmux-session.js` and `cmux-feed.js` snapshots generated by cmux 0.64.20 under a dedicated integration asset directory.
- [x] 2.2 Add English provenance documentation identifying cmux 0.64.20 and the snapshot generation source without adding runtime generation or download behavior.

## 3. Preflight and Installation

- [x] 3.1 Add an injectable selected-extra preflight that resolves `cmux` from `PATH` for both planning and application, skips the lookup when unselected, and fails clearly before any mutation when missing.
- [x] 3.2 Prepare both selected cmux assets for `<ConfigDir>/plugins/` through the shared prepared-file pipeline without creating or patching `opencode.json`.
- [x] 3.3 Confirm shared reconciliation handles divergent-file backups, independently recoverable partial application, unchanged files, and deselection without adding cmux-specific uninstall logic.

## 4. Installer Tests

- [x] 4.1 Add selection tests proving cmux starts unselected, existing extras keep their defaults, and explicit selection includes the integration.
- [x] 4.2 Add plan/apply tests proving both plugin targets are reported and installed from the vendored snapshots without modifying `opencode.json`.
- [x] 4.3 Add preflight tests for selected missing-cmux failure before mutations and unselected lookup bypass.
- [x] 4.4 Add reconciliation tests for divergent-content backups, idempotent reapplication without extra backups, partial retry convergence, and preservation after deselection.
- [x] 4.5 Run the installer test suite and keep coverage limited to selection, plan/apply, preflight, backups, and idempotency.
